machine:
  java:
    version: oraclejdk8
  timezone:
    Europe/London

dependencies:
 override: 
  - mvn dependency:resolve -s settings.xml

test:
  override:
    - mvn clean install -s settings.xml
